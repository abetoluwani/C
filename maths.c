#include<stdio.h>
#include<math.h>

int main (){
    double A = sqrt(9);
    // double B = pow(2,4);
    // int C = round (3.142);
    // int D = floor (3.99);
    // double E = sin(33);
    // double F = cos(90);
    // double G = tan (120);
    // double H = log(100);


    printf("%lf",A);
    

    return 0;
}